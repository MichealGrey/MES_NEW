<UserControl x:Class="MES.Shell.Views.NavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:models="clr-namespace:MES.Shell.Models"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource MesSurfaceBrush}">

    <!--
        NavigationView 左侧导航
        - 使用 ListBox 实现侧边导航
        - 点击叶子节点 → 导航到 MainContentRegion
    -->

    <Grid>
        <Grid.RowDefinitions>
            <!-- 导航标题 -->
            <RowDefinition Height="40"/>
            <!-- 导航菜单 -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 模块标题 -->
        <Border Grid.Row="0" Background="{StaticResource MesCardBrush}" Padding="16,0">
            <TextBlock Text="{Binding CurrentModuleTitle}"
                       Foreground="{StaticResource MesTextPrimaryBrush}" FontSize="14" FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
        </Border>

        <!--
            ListBox 侧边导航菜单
            - ItemsSource 绑定到 ViewModel 的 NavigationItems 集合
            - 选中项变化时触发导航
        -->
        <ListBox Grid.Row="1"
                 ItemsSource="{Binding NavigationItems}"
                 SelectionChanged="ListBox_SelectionChanged"
                 Margin="4,0"
                 Background="Transparent"
                 BorderThickness="0"
                 Foreground="{StaticResource MesTextPrimaryBrush}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{StaticResource MesTextPrimaryBrush}"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="Margin" Value="2,1"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="Bd"
                                        Background="{TemplateBinding Background}"
                                        Padding="{TemplateBinding Padding}"
                                        CornerRadius="4"
                                        SnapsToDevicePixels="True">
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#35355A"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#303055"/>
                                        <Setter Property="Foreground" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:NavigationItem}">
                    <StackPanel Orientation="Horizontal" Margin="4,2">
                        <!-- 图标 -->
                        <TextBlock Text="{Binding Icon}" FontSize="14"
                                   VerticalAlignment="Center" Margin="0,0,8,0"
                                   Foreground="{StaticResource MesTextMutedBrush}"/>
                        <!-- 标题 -->
                        <TextBlock Text="{Binding Title}" FontSize="13"
                                   VerticalAlignment="Center"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
