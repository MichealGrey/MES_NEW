<UserControl x:Class="MES.Shell.Views.Templates.StandardPageTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <!-- 页面标题和操作按钮区 -->
            <RowDefinition Height="Auto"/>
            <!-- 统计概览区（可选） -->
            <RowDefinition Height="Auto"/>
            <!-- 过滤器区域 -->
            <RowDefinition Height="Auto"/>
            <!-- 主数据表格 -->
            <RowDefinition Height="*"/>
            <!-- 分页器 -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 页面标题和操作按钮 -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                      Text="{Binding PageTitle}"
                      FontFamily="{DynamicResource MesFontDisplay}"
                      FontSize="24"
                      FontWeight="SemiBold"
                      Foreground="{DynamicResource MesTextPrimaryBrush}"
                      VerticalAlignment="Center"/>

            <ItemsControl Grid.Column="1"
                         ItemsSource="{Binding ActionButtons}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>

        <!-- 统计概览区 -->
        <ItemsControl Grid.Row="1"
                     ItemsSource="{Binding KpiCards}"
                     Margin="0,0,0,24">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>

        <!-- 过滤器区域 -->
        <Border Grid.Row="2"
               Style="{StaticResource MesCard}"
               Margin="0,0,0,24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ItemsControl Grid.Column="0"
                             ItemsSource="{Binding FilterControls}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>

                <StackPanel Grid.Column="1"
                           Orientation="Horizontal"
                           VerticalAlignment="Center">
                    <Button Content="搜索"
                           Style="{StaticResource MesPrimaryButton}"
                           Margin="0,0,8,0"/>
                    <Button Content="重置"
                           Style="{StaticResource MesSecondaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主数据表格 -->
        <Border Grid.Row="3"
               Style="{StaticResource MesCard}"
               Padding="0">
            <ContentControl Content="{Binding MainTableContent}"/>
        </Border>

        <!-- 分页器 -->
        <Border Grid.Row="4"
               Style="{StaticResource MesCard}"
               Margin="0,16,0,0"
               Padding="16">
            <ContentControl Content="{Binding PaginationControl}"/>
        </Border>
    </Grid>
</UserControl>
