<UserControl x:Class="MES.Modules.Alarm.Views.AlarmHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource MesBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="告警历史" FontSize="20" FontWeight="Bold" Foreground="{StaticResource MesTextPrimaryBrush}" Margin="0,0,0,12"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{StaticResource MesSurfaceBrush}" Foreground="{StaticResource MesTextPrimaryBrush}"
                  BorderBrush="{StaticResource MesBorderBrush}" RowBackground="{StaticResource MesCardBrush}"
                  AlternatingRowBackground="{StaticResource MesSurfaceBrush}" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="告警ID" Binding="{Binding AlarmId}" Width="100"/>
                <DataGridTextColumn Header="设备" Binding="{Binding EquipmentId}" Width="100"/>
                <DataGridTextColumn Header="级别" Binding="{Binding Severity}" Width="80"/>
                <DataGridTextColumn Header="内容" Binding="{Binding Text}" Width="*"/>
                <DataGridTextColumn Header="确认人" Binding="{Binding AckBy}" Width="80"/>
                <DataGridTextColumn Header="关闭时间" Binding="{Binding CloseTime, StringFormat='{}{0:HH:mm:ss}'}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
