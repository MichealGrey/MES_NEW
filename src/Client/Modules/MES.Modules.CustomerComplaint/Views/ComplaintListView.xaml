<UserControl x:Class="MES.Modules.CustomerComplaint.Views.ComplaintListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MesCardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>
        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource MesPrimaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource MesTextSecondaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style x:Key="StatValueSmallStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource MesPrimaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="StatLabelSmallStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Foreground" Value="{DynamicResource MesTextSecondaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,2,0,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 统计卡片 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatNumberStyle}" Text="{Binding TotalCount}"/>
                    <TextBlock Style="{StaticResource StatLabelStyle}" Text="总计"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatNumberStyle}" Text="{Binding OpenCount}" Foreground="Blue"/>
                    <TextBlock Style="{StaticResource StatLabelStyle}" Text="开放"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatNumberStyle}" Text="{Binding InProgressCount}" Foreground="Orange"/>
                    <TextBlock Style="{StaticResource StatLabelStyle}" Text="进行中"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatNumberStyle}" Text="{Binding ClosedCount}" Foreground="Green"/>
                    <TextBlock Style="{StaticResource StatLabelStyle}" Text="已关闭"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatNumberStyle}" Text="{Binding OverdueCount}" Foreground="Red"/>
                    <TextBlock Style="{StaticResource StatLabelStyle}" Text="逾期"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 第二行统计卡片 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatValueSmallStyle}" Text="{Binding AvgOpenDays, StringFormat={}{0:F1}}"/>
                    <TextBlock Style="{StaticResource StatLabelSmallStyle}" Text="平均开放天数"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatValueSmallStyle}" Text="{Binding CloseRate, StringFormat={}{0:F1}%}"/>
                    <TextBlock Style="{StaticResource StatLabelSmallStyle}" Text="结案率"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 工具栏 -->
        <Grid Grid.Row="2" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <!-- 搜索框 -->
            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="8" VerticalContentAlignment="Center">
                <TextBox.Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="{DynamicResource MesCardBackgroundBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource MesBorderBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource MesTextPrimaryBrush}"/>
                    </Style>
                </TextBox.Style>
            </TextBox>
            <!-- 状态过滤 -->
            <ComboBox Grid.Column="1" Text="{Binding FilterStatus, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" VerticalContentAlignment="Center">
                <ComboBox.ItemsSource>
                    <x:Array Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                        <sys:String>Open</sys:String>
                        <sys:String>InProgress</sys:String>
                        <sys:String>Closed</sys:String>
                    </x:Array>
                </ComboBox.ItemsSource>
            </ComboBox>
            <!-- 严重度过滤 -->
            <ComboBox Grid.Column="2" Text="{Binding FilterSeverity, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" VerticalContentAlignment="Center">
                <ComboBox.ItemsSource>
                    <x:Array Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                        <sys:String>Low</sys:String>
                        <sys:String>Medium</sys:String>
                        <sys:String>High</sys:String>
                        <sys:String>Critical</sys:String>
                    </x:Array>
                </ComboBox.ItemsSource>
            </ComboBox>
            <!-- 8D进度过滤 -->
            <ComboBox Grid.Column="3" Text="{Binding FilterEightDStatus, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" VerticalContentAlignment="Center" ToolTip="8D进度过滤">
                <ComboBox.ItemsSource>
                    <x:Array Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                        <sys:String>D0</sys:String>
                        <sys:String>D1</sys:String>
                        <sys:String>D2</sys:String>
                        <sys:String>D3</sys:String>
                        <sys:String>D4</sys:String>
                        <sys:String>D5</sys:String>
                        <sys:String>D6</sys:String>
                        <sys:String>D7</sys:String>
                        <sys:String>D8</sys:String>
                    </x:Array>
                </ComboBox.ItemsSource>
            </ComboBox>
            <!-- 优先级过滤 -->
            <ComboBox Grid.Column="4" Text="{Binding FilterPriority, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" VerticalContentAlignment="Center" ToolTip="优先级过滤">
                <ComboBox.ItemsSource>
                    <x:Array Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                        <sys:String>Low</sys:String>
                        <sys:String>Normal</sys:String>
                        <sys:String>High</sys:String>
                        <sys:String>Urgent</sys:String>
                    </x:Array>
                </ComboBox.ItemsSource>
            </ComboBox>
            <!-- 仅显示逾期 -->
            <CheckBox Grid.Column="5" Content="仅逾期" IsChecked="{Binding ShowOverdueOnly}" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="Red" FontWeight="SemiBold"/>
            <!-- 操作按钮 -->
            <Button Grid.Column="7" Content="新建" Command="{Binding AddCommand}" Margin="8,0,4,0" Width="70"/>
            <Button Grid.Column="8" Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,4,0" Width="70"/>
            <Button Grid.Column="9" Content="清除" Command="{Binding ClearFilterCommand}" Width="70"/>
        </Grid>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Complaints}" SelectedItem="{Binding SelectedComplaint}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" GridLinesVisibility="Horizontal" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="客诉单号" Binding="{Binding ComplaintNo}" Width="130"/>
                <DataGridTextColumn Header="客户" Binding="{Binding CustomerName}" Width="120"/>
                <DataGridTextColumn Header="产品" Binding="{Binding ProductName}" Width="120"/>
                <DataGridTextColumn Header="缺陷类型" Binding="{Binding DefectType}" Width="100"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="严重度" Binding="{Binding Severity}" Width="70"/>
                <DataGridTextColumn Header="优先级" Binding="{Binding Priority}" Width="70"/>
                <DataGridTextColumn Header="8D进度" Binding="{Binding EightDStatus}" Width="70"/>
                <DataGridTextColumn Header="退货数" Binding="{Binding ReturnQty}" Width="70"/>
                <DataGridTextColumn Header="开放天数" Binding="{Binding OpenDays}" Width="70"/>
                <DataGridTextColumn Header="逾期天数" Binding="{Binding OverdueDays}" Width="70"/>
                <DataGridTextColumn Header="报告日期" Binding="{Binding ReportDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="要求日期" Binding="{Binding RequiredDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTemplateColumn Header="操作" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="详情" Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Margin="2" Padding="6,2"/>
                                <Button Content="编辑" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Margin="2" Padding="6,2"/>
                                <Button Content="关闭" Command="{Binding DataContext.CloseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Margin="2" Padding="6,2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="Closed">
                            <Setter Property="Foreground" Value="{DynamicResource MesTextSecondaryBrush}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding OverdueDays}" Value="0">
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   FontSize="16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                   TextWrapping="Wrap"/>
    </Grid>
</UserControl>
