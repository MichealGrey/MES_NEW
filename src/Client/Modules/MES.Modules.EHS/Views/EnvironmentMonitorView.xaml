<UserControl x:Class="MES.Modules.EHS.Views.EnvironmentMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource MesBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="环境监控" FontSize="20" FontWeight="Bold" Foreground="{StaticResource MesTextPrimaryBrush}" Margin="0,0,0,12"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{StaticResource MesSurfaceBrush}" Foreground="{StaticResource MesTextPrimaryBrush}"
                  BorderBrush="{StaticResource MesBorderBrush}" RowBackground="{StaticResource MesCardBrush}"
                  AlternatingRowBackground="{StaticResource MesSurfaceBrush}" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="区域" Binding="{Binding Area}" Width="120"/>
                <DataGridTextColumn Header="温度" Binding="{Binding Temperature}" Width="80"/>
                <DataGridTextColumn Header="湿度" Binding="{Binding Humidity}" Width="80"/>
                <DataGridTextColumn Header="颗粒数" Binding="{Binding Particles}" Width="80"/>
                <DataGridTextColumn Header="温度状态" Binding="{Binding TempStatus}" Width="80"/>
	                <DataGridTextColumn Header="湿度状态" Binding="{Binding HumidityStatus}" Width="80"/>
	                <DataGridTextColumn Header="颗粒状态" Binding="{Binding ParticleStatus}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
