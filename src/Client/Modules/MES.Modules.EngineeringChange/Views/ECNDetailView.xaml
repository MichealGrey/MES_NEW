<UserControl x:Class="MES.Modules.EngineeringChange.Views.ECNDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 头部信息卡片 -->
        <Border Grid.Row="0" Background="{DynamicResource MesCardBackgroundBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Text="{Binding ECN.EcnNo}"/>
                        <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Text="{Binding ECN.EcnTitle}" Margin="12,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="类型: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                        <TextBlock Text="{Binding ECN.EcnType}" FontSize="12" Margin="0,0,16,0"/>
                        <TextBlock Text="状态: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                        <TextBlock Text="{Binding ECN.Status}" FontSize="12" Margin="0,0,16,0"/>
                        <TextBlock Text="紧急度: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                        <TextBlock Text="{Binding ECN.Urgency}" FontSize="12" Margin="0,0,16,0"/>
                        <TextBlock Text="风险: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                        <TextBlock Text="{Binding ECN.RiskLevel}" FontSize="12"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock Text="申请人: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                        <TextBlock Text="{Binding ECN.RequestedBy}" FontSize="12" Margin="0,0,16,0"/>
                        <TextBlock Text="申请日期: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                        <TextBlock Text="{Binding ECN.RequestedAt, StringFormat=yyyy-MM-dd}" FontSize="12" Margin="0,0,16,0"/>
                        <TextBlock Text="生效日期: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                        <TextBlock Text="{Binding ECN.EffectiveDate, StringFormat=yyyy-MM-dd}" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                    <Button Content="刷新" Command="{Binding RefreshCommand}" Width="70" Margin="0,0,8,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 变更原因 -->
        <Border Grid.Row="1" Background="{DynamicResource MesCardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="变更原因" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,6"/>
                <TextBlock Text="{Binding ECN.Reason}" TextWrapping="Wrap" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- 子表数据区域 -->
        <TabControl Grid.Row="2">
            <!-- 变更项 -->
            <TabItem Header="变更项">
                <DataGrid ItemsSource="{Binding ECN.Items}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="类型" Binding="{Binding ItemType}" Width="80"/>
                        <DataGridTextColumn Header="物料编码" Binding="{Binding ItemCode}" Width="120"/>
                        <DataGridTextColumn Header="物料名称" Binding="{Binding ItemName}" Width="150"/>
                        <DataGridTextColumn Header="变更前" Binding="{Binding OldValue}" Width="150"/>
                        <DataGridTextColumn Header="变更后" Binding="{Binding NewValue}" Width="150"/>
                        <DataGridTextColumn Header="变更原因" Binding="{Binding ChangeReason}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- 影响评估 -->
            <TabItem Header="影响评估">
                <DataGrid ItemsSource="{Binding ECN.Impacts}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="影响类型" Binding="{Binding ImpactType}" Width="100"/>
                        <DataGridTextColumn Header="严重程度" Binding="{Binding Severity}" Width="80"/>
                        <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="*"/>
                        <DataGridTextColumn Header="应对措施" Binding="{Binding Action}" Width="200"/>
                        <DataGridTextColumn Header="责任人" Binding="{Binding Responsible}" Width="100"/>
                        <DataGridTextColumn Header="截止日期" Binding="{Binding DueDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- 审批记录 -->
            <TabItem Header="审批记录">
                <DataGrid ItemsSource="{Binding ECN.Approvers}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="审批人" Binding="{Binding ApproverName}" Width="100"/>
                        <DataGridTextColumn Header="角色" Binding="{Binding Role}" Width="80"/>
                        <DataGridTextColumn Header="顺序" Binding="{Binding ApprovalOrder}" Width="60"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                        <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="80"/>
                        <DataGridTextColumn Header="意见" Binding="{Binding Comments}" Width="*"/>
                        <DataGridTextColumn Header="审批时间" Binding="{Binding ApprovedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- 通知部门 -->
            <TabItem Header="通知部门">
                <DataGrid ItemsSource="{Binding ECN.NotifyDepts}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="部门" Binding="{Binding DeptName}" Width="150"/>
                        <DataGridTextColumn Header="已确认" Binding="{Binding Confirmed}" Width="80"/>
                        <DataGridTextColumn Header="通知时间" Binding="{Binding NotifiedAt, StringFormat=yyyy-MM-dd}" Width="120"/>
                        <DataGridTextColumn Header="确认人" Binding="{Binding ConfirmedBy}" Width="100"/>
                        <DataGridTextColumn Header="确认时间" Binding="{Binding ConfirmedAt, StringFormat=yyyy-MM-dd}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- 实施记录 -->
            <TabItem Header="实施记录">
                <DataGrid ItemsSource="{Binding ECN.Implements}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="任务名称" Binding="{Binding TaskName}" Width="150"/>
                        <DataGridTextColumn Header="责任人" Binding="{Binding Responsible}" Width="100"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                        <DataGridTextColumn Header="计划日期" Binding="{Binding PlanDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                        <DataGridTextColumn Header="实际日期" Binding="{Binding ActualDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                        <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <!-- 变更历史 -->
            <TabItem Header="变更历史">
                <DataGrid ItemsSource="{Binding History}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="操作" Binding="{Binding Action}" Width="100"/>
                        <DataGridTextColumn Header="操作人" Binding="{Binding ActionBy}" Width="100"/>
                        <DataGridTextColumn Header="从状态" Binding="{Binding FromStatus}" Width="80"/>
                        <DataGridTextColumn Header="到状态" Binding="{Binding ToStatus}" Width="80"/>
                        <DataGridTextColumn Header="备注" Binding="{Binding Comment}" Width="*"/>
                        <DataGridTextColumn Header="时间" Binding="{Binding ActionDate, StringFormat=yyyy-MM-dd HH:mm}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   FontSize="16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                   TextWrapping="Wrap"/>
    </Grid>
</UserControl>
