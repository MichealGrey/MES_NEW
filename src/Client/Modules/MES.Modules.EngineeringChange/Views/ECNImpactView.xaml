<UserControl x:Class="MES.Modules.EngineeringChange.Views.ECNImpactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MesCardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>
        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource MesPrimaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource MesTextSecondaryBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题和刷新 -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="工程变更 - 影响分析" FontSize="20" FontWeight="SemiBold"/>
            <Button Grid.Column="1" Content="刷新" Command="{Binding RefreshCommand}" Width="70"/>
        </Grid>

        <!-- 统计卡片 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatNumberStyle}" Text="{Binding TotalApproved}"/>
                    <TextBlock Style="{StaticResource StatLabelStyle}" Text="已结案总数"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource StatNumberStyle}" Text="{Binding AffectedProducts}"/>
                    <TextBlock Style="{StaticResource StatLabelStyle}" Text="影响产品数"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding ApprovedECNs}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ECN单号" Binding="{Binding EcnNo}" Width="130"/>
                <DataGridTextColumn Header="标题" Binding="{Binding EcnTitle}" Width="200"/>
                <DataGridTextColumn Header="类型" Binding="{Binding EcnType}" Width="90"/>
                <DataGridTextColumn Header="影响产品" Binding="{Binding AffectedProducts}" Width="150"/>
                <DataGridTextColumn Header="影响工艺路线" Binding="{Binding AffectedRoutes}" Width="150"/>
                <DataGridTextColumn Header="变更原因" Binding="{Binding Reason}" Width="*"/>
                <DataGridTextColumn Header="生效日期" Binding="{Binding EffectiveDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTemplateColumn Header="操作" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="分析" Command="{Binding DataContext.AnalyzeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" Margin="2" Padding="8,4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="2" Text="{Binding ErrorMessage}" Foreground="Red" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   FontSize="16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"
                   TextWrapping="Wrap"/>
    </Grid>
</UserControl>
