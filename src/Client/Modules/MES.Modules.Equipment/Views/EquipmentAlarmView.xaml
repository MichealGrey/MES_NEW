<UserControl x:Class="MES.Modules.Equipment.Views.EquipmentAlarmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="设备告警" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,0,16"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Alarms}" AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{DynamicResource MesDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="告警ID" Binding="{Binding AlarmId}" Width="100"/>
                <DataGridTextColumn Header="设备" Binding="{Binding EquipmentId}" Width="100"/>
                <DataGridTextColumn Header="告警码" Binding="{Binding AlarmCode}" Width="80"/>
                <DataGridTextColumn Header="级别" Binding="{Binding Severity}" Width="80"/>
                <DataGridTextColumn Header="内容" Binding="{Binding Text}" Width="*"/>
                <DataGridTextColumn Header="时间" Binding="{Binding Time, StringFormat='{}{0:HH:mm:ss}'}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
