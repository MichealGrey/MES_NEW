<UserControl x:Class="MES.Modules.Equipment.Views.EquipmentOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="设备状态总览" FontSize="24" FontWeight="SemiBold"
                   Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Equipments}" AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{DynamicResource MesDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="设备ID" Binding="{Binding EquipmentId}" Width="80"/>
                <DataGridTextColumn Header="设备名" Binding="{Binding Name}" Width="120"/>
                <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="区域" Binding="{Binding Area}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="当前批次" Binding="{Binding CurrentLot}" Width="120"/>
                <DataGridTextColumn Header="配方" Binding="{Binding RecipeId}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
