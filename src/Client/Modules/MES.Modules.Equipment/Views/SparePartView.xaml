<UserControl x:Class="MES.Modules.Equipment.Views.SparePartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="备件管理" Style="{DynamicResource MesPageTitle}" Margin="0,0,0,16"/>

        <Border Grid.Row="1" Style="{DynamicResource MesCard}" Padding="16" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Width="200" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,16,0"/>
                <TextBlock Text="库存预警" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBlock Text="{Binding LowStockCount}" FontWeight="Bold" Foreground="Red" VerticalAlignment="Center"/>
                <TextBlock Text=" 项" VerticalAlignment="Center" Margin="0,0,0,0"/>
                <TextBlock Text=" | 刷新: " VerticalAlignment="Center" Margin="16,0,0,0"/>
                <Button Content="刷新数据" Command="{Binding RefreshCommand}" Padding="12,4" Margin="4,0,0,0"/>
                <Button Content="保存" Command="{Binding SaveCommand}" Padding="12,4" Margin="8,0,0,0"/>
                <Button Content="删除" Command="{Binding DeleteCommand}" Padding="12,4" Margin="8,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{DynamicResource MesCard}" Padding="16" Margin="0,0,0,0">
            <DataGrid ItemsSource="{Binding FilteredView}" SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="备件编号" Binding="{Binding PartNo}" Width="100"/>
                    <DataGridTextColumn Header="备件名称" Binding="{Binding PartName}" Width="150"/>
                    <DataGridTextColumn Header="适用设备" Binding="{Binding EquipmentType}" Width="120"/>
                    <DataGridTextColumn Header="库存数量" Binding="{Binding StockQty}" Width="80"/>
                    <DataGridTextColumn Header="最低库存" Binding="{Binding MinQty}" Width="80"/>
                    <DataGridTextColumn Header="最高库存" Binding="{Binding MaxQty}" Width="80"/>
                    <DataGridTextColumn Header="供应商" Binding="{Binding Supplier}" Width="120"/>
                    <DataGridTextColumn Header="存放位置" Binding="{Binding Location}" Width="100"/>
                    <DataGridTextColumn Header="单价" Binding="{Binding UnitPrice, StringFormat='{}{0:C2}'}" Width="100"/>
                    <DataGridTemplateColumn Header="库存状态" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="正常"/>
                                            <Setter Property="Foreground" Value="Green"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding StockQty}" Value="{Binding MinQty, RelativeSource={RelativeSource Self}}">
                                                    <Setter Property="Text" Value="低于最低库存"/>
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
