<UserControl x:Class="MES.Modules.Production.Views.AlarmDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:MES.Modules.Production.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <UserControl.Resources>
        <local:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="报警看板" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,24,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{DynamicResource MesPrimaryButton}" Margin="0,0,8,0"/>
            <Button Content="全部确认" Command="{Binding AcknowledgeAllCommand}" Style="{DynamicResource MesDangerButton}"/>
        </StackPanel>

        <!-- 统计卡片 -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,16">
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="未处理" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding UnresolvedCount}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesDangerBrush}"/>
                    <TextBlock Text="条报警" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="今日新增" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TodayCount}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesWarningBrush}"/>
                    <TextBlock Text="条报警" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="已解决" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding ResolvedCount}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesSuccessBrush}"/>
                    <TextBlock Text="条报警" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,0,0">
                <StackPanel>
                    <TextBlock Text="平均响应" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding AvgResponseTime}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesInfoBrush}"/>
                    <TextBlock Text="分钟" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <!-- 过滤栏 -->
        <Border Grid.Row="2" Style="{DynamicResource MesCard}" Padding="16" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="过滤:" VerticalAlignment="Center" Foreground="{DynamicResource MesTextSecondaryBrush}" Margin="0,0,8,0"/>
                <ComboBox Width="120" Style="{DynamicResource MesComboBox}" SelectedIndex="{Binding FilterSeverityIndex}" Margin="0,0,12,0">
                    <ComboBoxItem Content="全部级别"/>
                    <ComboBoxItem Content="Critical"/>
                    <ComboBoxItem Content="Error"/>
                    <ComboBoxItem Content="Warning"/>
                    <ComboBoxItem Content="Info"/>
                </ComboBox>
                <ComboBox Width="140" Style="{DynamicResource MesComboBox}" SelectedIndex="{Binding FilterTypeIndex}" Margin="0,0,12,0">
                    <ComboBoxItem Content="全部类型"/>
                    <ComboBoxItem Content="低良率"/>
                    <ComboBoxItem Content="超时"/>
                    <ComboBoxItem Content="设备故障"/>
                    <ComboBoxItem Content="队列积压"/>
                </ComboBox>
                <CheckBox Content="仅显示未处理" IsChecked="{Binding ShowUnresolvedOnly}" VerticalAlignment="Center" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- 报警列表 -->
        <Border Grid.Row="3" Style="{DynamicResource MesCard}" Padding="0">
            <DataGrid ItemsSource="{Binding FilteredAlarms}" AutoGenerateColumns="False" IsReadOnly="True"
                      Style="{DynamicResource MesDataGrid}"
                      FontSize="12" SelectionMode="Single"
                      CanUserSortColumns="True" CanUserResizeColumns="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="报警类型" Binding="{Binding AlarmType}" Width="120"/>
                    <DataGridTemplateColumn Header="级别" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="6,2" HorizontalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border" BasedOn="{StaticResource MesBadge}">
                                            <Setter Property="Background" Value="{DynamicResource MesTextTertiaryBrush}"/>
                                            <Setter Property="BorderBrush" Value="{DynamicResource MesTextTertiaryBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Severity}" Value="Critical">
                                                    <Setter Property="Background" Value="{DynamicResource MesDangerBrush}"/>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource MesDangerBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Severity}" Value="Error">
                                                    <Setter Property="Background" Value="{DynamicResource MesDangerBrush}"/>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource MesDangerBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                                    <Setter Property="Background" Value="{DynamicResource MesWarningBrush}"/>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource MesWarningBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Severity}" Value="Info">
                                                    <Setter Property="Background" Value="{DynamicResource MesInfoBrush}"/>
                                                    <Setter Property="BorderBrush" Value="{DynamicResource MesInfoBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Severity}" FontSize="10" FontWeight="SemiBold" Foreground="White"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
                    <DataGridTextColumn Header="批次" Binding="{Binding LotId}" Width="100"/>
                    <DataGridTextColumn Header="设备" Binding="{Binding EquipmentId}" Width="100"/>
                    <DataGridTextColumn Header="触发时间" Binding="{Binding TriggeredAt}" Width="150"/>
                    <DataGridTextColumn Header="状态" Width="80">
                        <DataGridTextColumn.Binding>
                            <Binding Path="IsAcknowledged">
                                <Binding.Converter>
                                    <local:BoolToStatusConverter/>
                                </Binding.Converter>
                            </Binding>
                        </DataGridTextColumn.Binding>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="操作" Width="80">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="确认" Command="{Binding DataContext.AcknowledgeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" Padding="4,2" FontSize="11">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MesPrimaryButton}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsAcknowledged}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
