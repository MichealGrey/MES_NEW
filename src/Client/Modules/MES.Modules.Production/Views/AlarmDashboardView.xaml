<UserControl x:Class="MES.Modules.Production.Views.AlarmDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:local="clr-namespace:MES.Modules.Production.Views"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#1E1E2E">
    <UserControl.Resources>
        <local:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
    </UserControl.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="报警看板" FontSize="22" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,24,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Width="90" Height="32" Margin="0,0,8,0"/>
            <Button Content="全部确认" Command="{Binding AcknowledgeAllCommand}" Width="100" Height="32"/>
        </StackPanel>

        <!-- 统计卡片 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- 未处理报警 -->
            <Border Grid.Column="0" Background="#45475A" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="未处理" FontSize="13" Foreground="#A6ADC8" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding UnresolvedCount}" FontSize="28" FontWeight="Bold" Foreground="#F38BA8"/>
                    <TextBlock Text="条报警" FontSize="11" Foreground="#6C7086" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- 今日报警 -->
            <Border Grid.Column="1" Background="#45475A" CornerRadius="8" Padding="16" Margin="4,0,4,0">
                <StackPanel>
                    <TextBlock Text="今日新增" FontSize="13" Foreground="#A6ADC8" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding TodayCount}" FontSize="28" FontWeight="Bold" Foreground="#F9E2AF"/>
                    <TextBlock Text="条报警" FontSize="11" Foreground="#6C7086" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- 已解决 -->
            <Border Grid.Column="2" Background="#45475A" CornerRadius="8" Padding="16" Margin="4,0,4,0">
                <StackPanel>
                    <TextBlock Text="已解决" FontSize="13" Foreground="#A6ADC8" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding ResolvedCount}" FontSize="28" FontWeight="Bold" Foreground="#A6E3A1"/>
                    <TextBlock Text="条报警" FontSize="11" Foreground="#6C7086" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- 平均响应时间 -->
            <Border Grid.Column="3" Background="#45475A" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="平均响应" FontSize="13" Foreground="#A6ADC8" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding AvgResponseTime}" FontSize="28" FontWeight="Bold" Foreground="#89B4FA"/>
                    <TextBlock Text="分钟" FontSize="11" Foreground="#6C7086" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 过滤栏 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="过滤:" VerticalAlignment="Center" Foreground="#A6ADC8" Margin="0,0,8,0"/>
            <ComboBox Width="120" Height="30" SelectedIndex="{Binding FilterSeverityIndex}" Margin="0,0,12,0">
                <ComboBoxItem Content="全部级别"/>
                <ComboBoxItem Content="Critical"/>
                <ComboBoxItem Content="Error"/>
                <ComboBoxItem Content="Warning"/>
                <ComboBoxItem Content="Info"/>
            </ComboBox>
            <ComboBox Width="140" Height="30" SelectedIndex="{Binding FilterTypeIndex}" Margin="0,0,12,0">
                <ComboBoxItem Content="全部类型"/>
                <ComboBoxItem Content="低良率"/>
                <ComboBoxItem Content="超时"/>
                <ComboBoxItem Content="设备故障"/>
                <ComboBoxItem Content="队列积压"/>
            </ComboBox>
            <CheckBox Content="仅显示未处理" IsChecked="{Binding ShowUnresolvedOnly}" VerticalAlignment="Center" Foreground="#CDD6F4"/>
        </StackPanel>

        <!-- 报警列表 -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding FilteredAlarms}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="#313244" Foreground="#CDD6F4" BorderBrush="#45475A"
                  RowBackground="#1E1E2E" AlternatingRowBackground="#313244" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="12" SelectionMode="Single"
                  CanUserSortColumns="True" CanUserResizeColumns="True">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,6"/>
                    <Setter Property="BorderBrush" Value="#585B70"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="36"/>
                    <Setter Property="Padding" Value="0,4"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#585B70"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Critical">
                            <Setter Property="Background" Value="#452830"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Severity}" Value="Error">
                            <Setter Property="Background" Value="#3D2028"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="报警类型" Binding="{Binding AlarmType}" Width="120"/>
                <DataGridTemplateColumn Header="级别" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#585B70"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Severity}" Value="Critical">
                                                <Setter Property="Background" Value="#F38BA8"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="Error">
                                                <Setter Property="Background" Value="#EBA0AC"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="Warning">
                                                <Setter Property="Background" Value="#F9E2AF"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Severity}" Value="Info">
                                                <Setter Property="Background" Value="#89B4FA"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Severity}" FontSize="10" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#1E1E2E"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Severity}" Value="Info">
                                                    <Setter Property="Foreground" Value="#1E1E2E"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
                <DataGridTextColumn Header="批次" Binding="{Binding LotId}" Width="100"/>
                <DataGridTextColumn Header="设备" Binding="{Binding EquipmentId}" Width="100"/>
                <DataGridTextColumn Header="触发时间" Binding="{Binding TriggeredAt}" Width="150"/>
                <DataGridTextColumn Header="状态" Width="80">
                    <DataGridTextColumn.Binding>
                        <Binding Path="IsAcknowledged">
                            <Binding.Converter>
                                <local:BoolToStatusConverter/>
                            </Binding.Converter>
                        </Binding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="操作" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="确认" Command="{Binding DataContext.AcknowledgeCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}" Width="60" Height="26" FontSize="11">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Visibility" Value="Visible"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAcknowledged}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
