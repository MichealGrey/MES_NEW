<UserControl x:Class="MES.Modules.Production.Views.CustomerManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="客户管理" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
            <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" Width="200" Height="28"
                     Style="{DynamicResource MesTextBox}" Margin="0,0,8,0"/>
            <Button Content="🔍 搜索" Command="{Binding RefreshCommand}" Height="28"
                    Style="{DynamicResource MesPrimaryButton}"/>
        </StackPanel>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="➕ 新增" Command="{Binding AddCommand}" Height="28"
                    Style="{DynamicResource MesPrimaryButton}"/>
            <Button Content="✏️ 编辑" Command="{Binding EditCommand}" Height="28" Margin="8,0,0,0"
                    Style="{DynamicResource MesSecondaryButton}"/>
            <Button Content="🔄 切换状态" Command="{Binding ToggleStatusCommand}" Height="28" Margin="8,0,0,0"
                    Style="{DynamicResource MesSecondaryButton}"/>
            <Button Content="🔄 刷新" Command="{Binding RefreshCommand}" Height="28" Margin="8,0,0,0"
                    Style="{DynamicResource MesSecondaryButton}"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False" IsReadOnly="True" Style="{DynamicResource MesDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="客户ID" Binding="{Binding CustomerId}" Width="120"/>
                <DataGridTextColumn Header="客户名称" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="客户代码" Binding="{Binding CustomerCode}" Width="100"/>
                <DataGridTextColumn Header="联系人" Binding="{Binding ContactPerson}" Width="100"/>
                <DataGridTextColumn Header="联系电话" Binding="{Binding ContactPhone}" Width="100"/>
                <DataGridTextColumn Header="质量等级" Binding="{Binding QualityLevel}" Width="100"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
