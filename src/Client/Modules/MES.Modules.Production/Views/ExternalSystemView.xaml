<UserControl x:Class="MES.Modules.Production.Views.ExternalSystemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#1E1E2E">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="外部系统接口" FontSize="22" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,24,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Width="90" Height="32" Margin="0,0,8,0"/>
            <Button Content="重发失败事件" Command="{Binding RetryFailedCommand}" Width="120" Height="32"/>
        </StackPanel>

        <!-- Tab 切换 -->
        <TabControl Grid.Row="1" SelectedIndex="{Binding SelectedTabIndex}" Margin="0,0,0,16">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                    <Setter Property="Padding" Value="16,8"/>
                    <Setter Property="FontSize" Value="13"/>
                </Style>
            </TabControl.Resources>
            <TabItem Header="事件队列"/>
            <TabItem Header="系统配置"/>
        </TabControl>

        <!-- 事件队列 -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Events}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="#313244" Foreground="#CDD6F4" BorderBrush="#45475A"
                  RowBackground="#1E1E2E" AlternatingRowBackground="#313244" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="12" CanUserSortColumns="True">
            <DataGrid.Style>
                <Style TargetType="DataGrid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="1">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Style>
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,6"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="36"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#585B70"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="事件类型" Binding="{Binding EventType}" Width="120"/>
                <DataGridTextColumn Header="目标系统" Binding="{Binding TargetSystem}" Width="100"/>
                <DataGridTemplateColumn Header="状态" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#585B70"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                <Setter Property="Background" Value="#F9E2AF"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Sent">
                                                <Setter Property="Background" Value="#A6E3A1"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Failed">
                                                <Setter Property="Background" Value="#F38BA8"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" FontSize="10" FontWeight="SemiBold" Foreground="#1E1E2E"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="重试次数" Binding="{Binding RetryCount}" Width="80"/>
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                <DataGridTextColumn Header="发送时间" Binding="{Binding SentAt, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                <DataGridTextColumn Header="错误信息" Binding="{Binding ErrorMessage}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 系统配置 -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding SystemConfigs}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="#313244" Foreground="#CDD6F4" BorderBrush="#45475A"
                  RowBackground="#1E1E2E" AlternatingRowBackground="#313244" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="12">
            <DataGrid.Style>
                <Style TargetType="DataGrid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedTabIndex}" Value="1">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Style>
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,6"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="36"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#585B70"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="系统ID" Binding="{Binding SystemId}" Width="100"/>
                <DataGridTextColumn Header="系统名称" Binding="{Binding SystemName}" Width="120"/>
                <DataGridTextColumn Header="类型" Binding="{Binding SystemType}" Width="80"/>
                <DataGridTextColumn Header="端点" Binding="{Binding Endpoint}" Width="*"/>
                <DataGridTextColumn Header="认证方式" Binding="{Binding AuthType}" Width="80"/>
                <DataGridTextColumn Header="超时(秒)" Binding="{Binding TimeoutSeconds}" Width="80"/>
                <DataGridTextColumn Header="最大重试" Binding="{Binding MaxRetries}" Width="80"/>
                <DataGridCheckBoxColumn Header="启用" Binding="{Binding IsEnabled}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
