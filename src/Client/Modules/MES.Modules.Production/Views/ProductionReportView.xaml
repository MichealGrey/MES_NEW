<UserControl x:Class="MES.Modules.Production.Views.ProductionReportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="生产报表" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,16,0"/>
            <TextBlock Text="日期:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
            <DatePicker SelectedDate="{Binding ReportDate}" Width="150" Margin="0,0,12,0"
                        Style="{DynamicResource MesDatePicker}"/>
            <Button Content="生成报表" Command="{Binding GenerateCommand}" Width="100"
                    Style="{DynamicResource MesPrimaryButton}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <TextBlock Text="加载中..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
        </Grid>

        <WrapPanel Grid.Row="2" Margin="0,0,0,16">
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="总批次" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Report.TotalLots}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="完成批次" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Report.CompletedLots}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesSuccessBrush}"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="总良率" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Report.OverallYield, StringFormat={}{0:F1}%}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesInfoBrush}"/>
                </StackPanel>
            </Border>
            <Border Style="{DynamicResource MesKpiCard}" Margin="0,0,0,0">
                <StackPanel>
                    <TextBlock Text="报废数量" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Report.TotalScrapQty}" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource MesDangerBrush}"/>
                </StackPanel>
            </Border>
        </WrapPanel>

        <DataGrid Grid.Row="3" ItemsSource="{Binding StepYields}" AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{DynamicResource MesDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="工序" Binding="{Binding StepCode}" Width="100"/>
                <DataGridTextColumn Header="名称" Binding="{Binding StepName}" Width="120"/>
                <DataGridTextColumn Header="投入" Binding="{Binding InputQty}" Width="100"/>
                <DataGridTextColumn Header="产出" Binding="{Binding PassQty}" Width="100"/>
                <DataGridTextColumn Header="良率" Binding="{Binding Yield, StringFormat={}{0:F1}%}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
