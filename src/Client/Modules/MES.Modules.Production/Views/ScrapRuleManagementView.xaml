<UserControl x:Class="MES.Modules.Production.Views.ScrapRuleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="报废规则管理" FontSize="24" FontWeight="SemiBold"
                       Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,0,16"
                       VerticalAlignment="Center"/>
            <Button Content="新增" Command="{Binding AddCommand}"
                    Style="{DynamicResource MesPrimaryButton}" Margin="0,0,8,0"/>
            <Button Content="编辑" Command="{Binding EditCommand}"
                    Style="{DynamicResource MesSecondaryButton}" Margin="0,0,8,0"/>
            <Button Content="删除" Command="{Binding DeleteCommand}"
                    Style="{DynamicResource MesDangerButton}" Margin="0,0,8,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}"
                    Style="{DynamicResource MesSecondaryButton}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding ScrapRules}" SelectedItem="{Binding SelectedScrapRule}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{DynamicResource MesDataGrid}"
                  SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="路线ID" Binding="{Binding RouteId}" Width="120"/>
                <DataGridTextColumn Header="工序代码" Binding="{Binding StepCode}" Width="100"/>
                <DataGridTextColumn Header="最大报废数量" Binding="{Binding MaxScrapQty}" Width="110"/>
                <DataGridTextColumn Header="最大报废比例%" Binding="{Binding MaxScrapPercent, StringFormat={}{0:F1}}" Width="120"/>
                <DataGridTextColumn Header="审批级别" Binding="{Binding ApprovalLevel}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
