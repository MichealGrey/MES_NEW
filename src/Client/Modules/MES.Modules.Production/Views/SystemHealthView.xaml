<UserControl x:Class="MES.Modules.Production.Views.SystemHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="系统健康监控" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,24,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{DynamicResource MesPrimaryButton}" Margin="0,0,8,0"/>
            <Button Content="清理过期数据" Command="{Binding CleanupCommand}" Style="{DynamicResource MesPrimaryButton}"/>
        </StackPanel>

        <!-- 整体状态 -->
        <Border Grid.Row="1" Padding="24" Margin="0,0,0,16">
            <Border.Style>
                <Style TargetType="Border" BasedOn="{StaticResource MesCard}">
                    <Setter Property="Background" Value="{DynamicResource MesSurfaceBrush}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Healthy">
                            <Setter Property="Background" Value="{DynamicResource MesSuccessBrush}"/>
                            <Setter Property="Opacity" Value="0.1"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Degraded">
                            <Setter Property="Background" Value="{DynamicResource MesWarningBrush}"/>
                            <Setter Property="Opacity" Value="0.1"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Critical">
                            <Setter Property="Background" Value="{DynamicResource MesDangerBrush}"/>
                            <Setter Property="Opacity" Value="0.1"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Ellipse Width="24" Height="24" Grid.Column="0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="{DynamicResource MesSuccessBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Degraded">
                                    <Setter Property="Fill" Value="{DynamicResource MesWarningBrush}"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Critical">
                                    <Setter Property="Fill" Value="{DynamicResource MesDangerBrush}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                    <TextBlock Text="系统状态" FontSize="13" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding HealthReport.OverallStatus}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="总批次" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}"/>
                        <TextBlock Text="{Binding HealthReport.TotalLots}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="活跃批次" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}"/>
                        <TextBlock Text="{Binding HealthReport.ActiveLots}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource MesInfoBrush}"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="未解决报警" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}"/>
                        <TextBlock Text="{Binding HealthReport.UnresolvedAlarms}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource MesDangerBrush}"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="数据异常" FontSize="11" Foreground="{DynamicResource MesTextTertiaryBrush}"/>
                        <TextBlock Text="{Binding HealthReport.DataInconsistencies}" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource MesWarningBrush}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 组件健康状态 -->
        <TextBlock Grid.Row="2" Text="组件状态" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="3" ItemsSource="{Binding HealthReport.Components}" AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{DynamicResource MesDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="组件名称" Binding="{Binding Name}" Width="150"/>
                <DataGridTemplateColumn Header="状态" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="{DynamicResource MesSuccessBrush}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Degraded">
                                                    <Setter Property="Fill" Value="{DynamicResource MesWarningBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Down">
                                                    <Setter Property="Fill" Value="{DynamicResource MesDangerBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding Status}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="响应时间" Binding="{Binding ResponseTimeMs, StringFormat={}{0:F0} ms}" Width="100"/>
                <DataGridTextColumn Header="最后检查" Binding="{Binding LastChecked, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="180"/>
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
