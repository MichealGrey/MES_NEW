<UserControl x:Class="MES.Modules.Production.Views.SystemHealthView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="#1E1E2E">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="系统健康监控" FontSize="22" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,24,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Width="90" Height="32" Margin="0,0,8,0"/>
            <Button Content="清理过期数据" Command="{Binding CleanupCommand}" Width="110" Height="32"/>
        </StackPanel>

        <!-- 整体状态 -->
        <Border Grid.Row="1" CornerRadius="12" Padding="24" Margin="0,0,0,16">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#45475A"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Healthy">
                            <Setter Property="Background" Value="#1B3A2A"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Degraded">
                            <Setter Property="Background" Value="#3D3A1B"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Critical">
                            <Setter Property="Background" Value="#3D1B1B"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Ellipse Width="24" Height="24" Grid.Column="0">
                    <Ellipse.Style>
                        <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#A6E3A1"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Degraded">
                                    <Setter Property="Fill" Value="#F9E2AF"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding HealthReport.OverallStatus}" Value="Critical">
                                    <Setter Property="Fill" Value="#F38BA8"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Ellipse.Style>
                </Ellipse>
                <StackPanel Grid.Column="1" Margin="16,0,0,0">
                    <TextBlock Text="系统状态" FontSize="13" Foreground="#A6ADC8"/>
                    <TextBlock Text="{Binding HealthReport.OverallStatus}" FontSize="24" FontWeight="Bold" Foreground="#CDD6F4"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="总批次" FontSize="11" Foreground="#6C7086"/>
                        <TextBlock Text="{Binding HealthReport.TotalLots}" FontSize="18" FontWeight="SemiBold" Foreground="#CDD6F4"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="活跃批次" FontSize="11" Foreground="#6C7086"/>
                        <TextBlock Text="{Binding HealthReport.ActiveLots}" FontSize="18" FontWeight="SemiBold" Foreground="#89B4FA"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,24,0">
                        <TextBlock Text="未解决报警" FontSize="11" Foreground="#6C7086"/>
                        <TextBlock Text="{Binding HealthReport.UnresolvedAlarms}" FontSize="18" FontWeight="SemiBold" Foreground="#F38BA8"/>
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="数据异常" FontSize="11" Foreground="#6C7086"/>
                        <TextBlock Text="{Binding HealthReport.DataInconsistencies}" FontSize="18" FontWeight="SemiBold" Foreground="#F9E2AF"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 组件健康状态 -->
        <TextBlock Grid.Row="2" Text="组件状态" FontSize="16" FontWeight="SemiBold" Foreground="#CDD6F4" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="3" ItemsSource="{Binding HealthReport.Components}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="#313244" Foreground="#CDD6F4" BorderBrush="#45475A"
                  RowBackground="#1E1E2E" AlternatingRowBackground="#313244" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="12">
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#45475A"/>
                    <Setter Property="Foreground" Value="#CDD6F4"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,6"/>
                </Style>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="40"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#585B70"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="组件名称" Binding="{Binding Name}" Width="150"/>
                <DataGridTemplateColumn Header="状态" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="#A6E3A1"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Degraded">
                                                    <Setter Property="Fill" Value="#F9E2AF"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Down">
                                                    <Setter Property="Fill" Value="#F38BA8"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding Status}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="响应时间" Binding="{Binding ResponseTimeMs, StringFormat={}{0:F0} ms}" Width="100"/>
                <DataGridTextColumn Header="最后检查" Binding="{Binding LastChecked, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="180"/>
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
