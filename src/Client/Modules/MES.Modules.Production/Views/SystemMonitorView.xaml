<UserControl x:Class="MES.Modules.Production.Views.SystemMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="系统监控" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,16,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{DynamicResource MesPrimaryButton}"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <TextBlock Text="加载中..." HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Style="{DynamicResource MesKpiCard}" Padding="16">
                <StackPanel>
                    <TextBlock Text="加工中" FontSize="14" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Monitor.ProcessingLots}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesSuccessBrush}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="1" Style="{DynamicResource MesKpiCard}" Padding="16" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="等待中" FontSize="14" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Monitor.WaitingLots}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesWarningBrush}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="2" Style="{DynamicResource MesKpiCard}" Padding="16" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="Hold" FontSize="14" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Monitor.HoldLots}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesDangerBrush}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="3" Style="{DynamicResource MesKpiCard}" Padding="16" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="可用设备" FontSize="14" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Monitor.AvailableEquipments}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
                </StackPanel>
            </Border>
            <Border Grid.Column="4" Style="{DynamicResource MesKpiCard}" Padding="16" Margin="12,0,0,0">
                <StackPanel>
                    <TextBlock Text="可用载具" FontSize="14" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding Monitor.AvailableCarriers}" FontSize="24" FontWeight="SemiBold" Foreground="{DynamicResource MesTextPrimaryBrush}"/>
                </StackPanel>
            </Border>
        </Grid>

        <DataGrid Grid.Row="3" ItemsSource="{Binding Alerts}" AutoGenerateColumns="False" IsReadOnly="True"
                  Style="{DynamicResource MesDataGrid}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="类型" Binding="{Binding AlertType}" Width="120"/>
                <DataGridTextColumn Header="级别" Binding="{Binding Severity}" Width="80"/>
                <DataGridTextColumn Header="消息" Binding="{Binding Message}" Width="*"/>
                <DataGridTextColumn Header="触发时间" Binding="{Binding TriggeredAt}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
