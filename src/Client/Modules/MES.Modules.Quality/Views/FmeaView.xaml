<UserControl x:Class="MES.Modules.Quality.Views.FmeaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MesCardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 统计卡片 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding TotalCount}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource MesPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="分析项总数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding HighRiskCount}" FontSize="28" FontWeight="Bold" Foreground="Red" HorizontalAlignment="Center"/>
                    <TextBlock Text="高风险(RPN≥100)" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding MediumRiskCount}" FontSize="28" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center"/>
                    <TextBlock Text="中风险(50-99)" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding AvgRPN, StringFormat={}{0:F0}}" FontSize="28" FontWeight="Bold" Foreground="Blue" HorizontalAlignment="Center"/>
                    <TextBlock Text="平均RPN" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding MaxRPN}" FontSize="28" FontWeight="Bold" Foreground="Red" HorizontalAlignment="Center"/>
                    <TextBlock Text="最大RPN" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 工具栏 -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="8" VerticalContentAlignment="Center" ToolTip="搜索工序/失效模式/影响"/>
            <ComboBox Grid.Column="1" Text="{Binding RiskFilter, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" VerticalContentAlignment="Center">
                <x:Array Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                    <sys:String>All</sys:String>
                    <sys:String>High</sys:String>
                    <sys:String>Medium</sys:String>
                    <sys:String>Low</sys:String>
                </x:Array>
            </ComboBox>
            <Button Grid.Column="3" Content="新建" Command="{Binding NewCommand}" Margin="8,0,4,0" Width="70"/>
            <Button Grid.Column="4" Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,4,0" Width="70"/>
            <Button Grid.Column="5" Content="按RPN排序" Command="{Binding SortByRPNCommand}" Margin="0,0,4,0" Width="90"/>
            <Button Grid.Column="6" Content="删除" Command="{Binding DeleteCommand}" Margin="0,0,4,0" Width="70"/>
            <Button Grid.Column="7" Content="清除" Command="{Binding ClearFilterCommand}" Width="70"/>
        </Grid>

        <!-- 编辑表单 -->
        <Border Grid.Row="2" Background="{DynamicResource MesCardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="工序" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.ProcessStep}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,8,4">
                    <TextBlock Text="失效模式" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.FailureMode}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,8,4">
                    <TextBlock Text="失效影响" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.FailureEffect}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,0,4">
                    <TextBlock Text="责任人" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.Responsible}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="严重度(S)" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.Severity}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,8,4">
                    <TextBlock Text="频度(O)" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.Occurrence}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,8,4">
                    <TextBlock Text="探测度(D)" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.Detection}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,0,0,4">
                    <TextBlock Text="RPN" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding EditingItem.RPN}" FontSize="16" FontWeight="Bold" Foreground="Red" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,8,4">
                    <TextBlock Text="控制措施" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.ControlMeasure}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" Margin="0,0,0,4">
                    <TextBlock Text="改进措施" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.ImprovementAction}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="保存" Command="{Binding SaveCommand}" Width="70" Margin="0,0,8,0"/>
                    <Button Content="取消" Command="{Binding CancelCommand}" Width="70"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding FilteredView}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" GridLinesVisibility="Horizontal" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="工序" Binding="{Binding ProcessStep}" Width="120"/>
                <DataGridTextColumn Header="失效模式" Binding="{Binding FailureMode}" Width="130"/>
                <DataGridTextColumn Header="失效影响" Binding="{Binding FailureEffect}" Width="150"/>
                <DataGridTextColumn Header="S" Binding="{Binding Severity}" Width="40"/>
                <DataGridTextColumn Header="O" Binding="{Binding Occurrence}" Width="40"/>
                <DataGridTextColumn Header="D" Binding="{Binding Detection}" Width="40"/>
                <DataGridTextColumn Header="RPN" Binding="{Binding RPN}" Width="50"/>
                <DataGridTextColumn Header="控制措施" Binding="{Binding ControlMeasure}" Width="*"/>
                <DataGridTextColumn Header="责任人" Binding="{Binding Responsible}" Width="80"/>
                <DataGridTextColumn Header="改进措施" Binding="{Binding ImprovementAction}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
