<UserControl x:Class="MES.Modules.Quality.Views.InspectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="检验管理" FontSize="24" FontWeight="SemiBold"
                   Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  Style="{DynamicResource MesDataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="检验ID" Binding="{Binding InspectionId}" Width="80"/>
                <DataGridTextColumn Header="批次号" Binding="{Binding LotId}" Width="120"/>
                <DataGridTextColumn Header="产品" Binding="{Binding Product}" Width="80"/>
                <DataGridTextColumn Header="检验类型" Binding="{Binding InspectionType}" Width="100"/>
                <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="80"/>
                <DataGridTextColumn Header="检验员" Binding="{Binding Inspector}" Width="100"/>
                <DataGridTextColumn Header="检验时间" Binding="{Binding InspectTime}" Width="140"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
