<UserControl x:Class="MES.Modules.Quality.Views.MsaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MesCardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 统计卡片 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding TotalCount}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource MesPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="量具总数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding AcceptableCount}" FontSize="28" FontWeight="Bold" Foreground="Green" HorizontalAlignment="Center"/>
                    <TextBlock Text="可接受" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding ConditionalCount}" FontSize="28" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center"/>
                    <TextBlock Text="条件接受" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding UnacceptableCount}" FontSize="28" FontWeight="Bold" Foreground="Red" HorizontalAlignment="Center"/>
                    <TextBlock Text="不可接受" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding AvgGRR, StringFormat={}{0:F1}%}" FontSize="28" FontWeight="Bold" Foreground="Blue" HorizontalAlignment="Center"/>
                    <TextBlock Text="平均GRR" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding OverdueCalibrationCount}" FontSize="28" FontWeight="Bold" Foreground="Red" HorizontalAlignment="Center"/>
                    <TextBlock Text="超期未校准" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 工具栏 -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="130"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Padding="8" VerticalContentAlignment="Center" ToolTip="搜索量具名称/参数"/>
            <ComboBox Grid.Column="1" Text="{Binding StatusFilter, UpdateSourceTrigger=PropertyChanged}" Margin="8,0,0,0" VerticalContentAlignment="Center">
                <x:Array Type="sys:String" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                    <sys:String>All</sys:String>
                    <sys:String>Acceptable</sys:String>
                    <sys:String>Conditional</sys:String>
                    <sys:String>Unacceptable</sys:String>
                </x:Array>
            </ComboBox>
            <Button Grid.Column="3" Content="新建" Command="{Binding NewCommand}" Margin="8,0,4,0" Width="70"/>
            <Button Grid.Column="4" Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,4,0" Width="70"/>
            <Button Grid.Column="5" Content="校准" Command="{Binding CalibrateCommand}" Margin="0,0,4,0" Width="70"/>
            <Button Grid.Column="6" Content="删除" Command="{Binding DeleteCommand}" Margin="0,0,4,0" Width="70"/>
            <Button Grid.Column="7" Content="清除" Command="{Binding ClearFilterCommand}" Width="70"/>
        </Grid>

        <!-- 编辑表单 -->
        <Border Grid.Row="2" Background="{DynamicResource MesCardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="量具名称" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.GaugeName}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,8,4">
                    <TextBlock Text="测量参数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.Parameter}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,8,4">
                    <TextBlock Text="GRR(%)" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.GRR}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="0" Grid.Column="3" Margin="0,0,0,4">
                    <TextBlock Text="NDC" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.NDC}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,8,4">
                    <TextBlock Text="操作员数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.OperatorCount}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,8,4">
                    <TextBlock Text="零件数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.PartCount}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,8,4">
                    <TextBlock Text="试验次数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.TrialCount}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Grid.Column="3" Margin="0,0,0,4">
                    <TextBlock Text="最后校准日期" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <DatePicker SelectedDate="{Binding EditingItem.LastCalibration}" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Row="2" Grid.Column="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                    <Button Content="保存" Command="{Binding SaveCommand}" Width="70" Margin="0,0,8,0"/>
                    <Button Content="取消" Command="{Binding CancelCommand}" Width="70"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding FilteredView}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" GridLinesVisibility="Horizontal" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="量具名称" Binding="{Binding GaugeName}" Width="130"/>
                <DataGridTextColumn Header="测量参数" Binding="{Binding Parameter}" Width="120"/>
                <DataGridTextColumn Header="操作员数" Binding="{Binding OperatorCount}" Width="80"/>
                <DataGridTextColumn Header="零件数" Binding="{Binding PartCount}" Width="70"/>
                <DataGridTextColumn Header="试验次数" Binding="{Binding TrialCount}" Width="80"/>
                <DataGridTextColumn Header="GRR(%)" Binding="{Binding GRR}" Width="70"/>
                <DataGridTextColumn Header="NDC" Binding="{Binding NDC}" Width="60"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="最后校准" Binding="{Binding LastCalibration, StringFormat=yyyy-MM-dd}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
