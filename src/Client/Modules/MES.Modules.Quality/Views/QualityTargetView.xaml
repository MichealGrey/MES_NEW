<UserControl x:Class="MES.Modules.Quality.Views.QualityTargetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MesCardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 统计卡片 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding TotalCount}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource MesPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="目标总数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding OnTargetCount}" FontSize="28" FontWeight="Bold" Foreground="Green" HorizontalAlignment="Center"/>
                    <TextBlock Text="达标" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding AtRiskCount}" FontSize="28" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center"/>
                    <TextBlock Text="有风险" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding MissedCount}" FontSize="28" FontWeight="Bold" Foreground="Red" HorizontalAlignment="Center"/>
                    <TextBlock Text="未达标" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding OverallAchievementRate, StringFormat={}{0:F1}%}" FontSize="28" FontWeight="Bold" Foreground="Blue" HorizontalAlignment="Center"/>
                    <TextBlock Text="总体达成率" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 工具栏 -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="新建" Command="{Binding NewCommand}" HorizontalAlignment="Left" Width="70"/>
            <Button Grid.Column="1" Content="刷新" Command="{Binding RefreshCommand}" Margin="8,0,4,0" Width="70"/>
            <Button Grid.Column="2" Content="更新当前值" Command="{Binding UpdateCurrentValueCommand}" Margin="0,0,4,0" Width="90"/>
            <Button Grid.Column="3" Content="删除" Command="{Binding DeleteCommand}" Margin="0,0,4,0" Width="70"/>
        </Grid>

        <!-- 编辑表单 -->
        <Border Grid.Row="2" Background="{DynamicResource MesCardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsEditing}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,8,0">
                    <TextBlock Text="目标名称" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.TargetName}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0,0,8,0">
                    <TextBlock Text="目标值" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.TargetValue}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="0,0,8,0">
                    <TextBlock Text="当前值" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBox Text="{Binding EditingItem.CurrentValue}" Padding="6" Margin="0,2"/>
                </StackPanel>
                <StackPanel Grid.Column="3">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="保存" Command="{Binding SaveCommand}" Width="70" Margin="0,0,8,0"/>
                        <Button Content="取消" Command="{Binding CancelCommand}" Width="70"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" GridLinesVisibility="Horizontal" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="目标名称" Binding="{Binding TargetName}" Width="180"/>
                <DataGridTextColumn Header="当前值" Binding="{Binding CurrentValue}" Width="80"/>
                <DataGridTextColumn Header="目标值" Binding="{Binding TargetValue}" Width="80"/>
                <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="60"/>
                <DataGridTextColumn Header="周期" Binding="{Binding Period}" Width="60"/>
                <DataGridTextColumn Header="达成率" Binding="{Binding AchievementRate, StringFormat={}{0:F1}%}" Width="80"/>
                <DataGridTextColumn Header="趋势" Binding="{Binding Trend}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
