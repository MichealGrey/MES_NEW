<UserControl x:Class="MES.Modules.Quality.Views.SpcChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="SPC监控" FontSize="24" FontWeight="SemiBold"
                   Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  Style="{DynamicResource MesDataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="设备ID" Binding="{Binding EquipmentId}" Width="80"/>
                <DataGridTextColumn Header="参数" Binding="{Binding Parameter}" Width="140"/>
                <DataGridTextColumn Header="UCL" Binding="{Binding UCL}" Width="100"/>
                <DataGridTextColumn Header="CL" Binding="{Binding CL}" Width="100"/>
                <DataGridTextColumn Header="LCL" Binding="{Binding LCL}" Width="100"/>
                <DataGridTextColumn Header="最新值" Binding="{Binding LatestValue}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
