<UserControl x:Class="MES.Modules.Schedule.Views.CapacityAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource MesBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="产能分析" FontSize="20" FontWeight="Bold"
                   Foreground="{StaticResource MesTextPrimaryBrush}" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{StaticResource MesSurfaceBrush}" Foreground="{StaticResource MesTextPrimaryBrush}"
                  BorderBrush="{StaticResource MesBorderBrush}" RowBackground="{StaticResource MesCardBrush}"
                  AlternatingRowBackground="{StaticResource MesSurfaceBrush}" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="设备ID" Binding="{Binding EquipmentId}" Width="80"/>
                <DataGridTextColumn Header="设备名" Binding="{Binding EquipmentName}" Width="120"/>
                <DataGridTextColumn Header="类型" Binding="{Binding Type}" Width="100"/>
                <DataGridTextColumn Header="计划利用率(%)" Binding="{Binding PlannedUtilization}" Width="120"/>
                <DataGridTextColumn Header="实际利用率(%)" Binding="{Binding ActualUtilization}" Width="120"/>
                <DataGridTextColumn Header="在制品数" Binding="{Binding WipCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
