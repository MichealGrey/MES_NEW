<UserControl x:Class="MES.Modules.Schedule.Views.DispatchBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="派工看板" FontSize="24" FontWeight="SemiBold"
                   Foreground="{DynamicResource MesTextPrimaryBrush}" Margin="0,0,0,16"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  Style="{DynamicResource MesDataGrid}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="设备ID" Binding="{Binding EquipmentId}" Width="80"/>
                <DataGridTextColumn Header="批次号" Binding="{Binding LotId}" Width="120"/>
                <DataGridTextColumn Header="产品" Binding="{Binding Product}" Width="80"/>
                <DataGridTextColumn Header="优先级" Binding="{Binding Priority}" Width="80"/>
                <DataGridTextColumn Header="工序" Binding="{Binding Step}" Width="100"/>
                <DataGridTextColumn Header="配方" Binding="{Binding RecipeId}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
