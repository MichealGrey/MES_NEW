<UserControl x:Class="MES.Modules.SystemSettings.Views.SystemMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <UserControl.Resources>
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource MesCardBackgroundBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,12,0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 统计卡片 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding TotalServices}" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource MesPrimaryBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="服务总数" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding RunningServices}" FontSize="28" FontWeight="Bold" Foreground="Green" HorizontalAlignment="Center"/>
                    <TextBlock Text="运行中" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding DegradedServices}" FontSize="28" FontWeight="Bold" Foreground="Orange" HorizontalAlignment="Center"/>
                    <TextBlock Text="降级" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding StoppedServices}" FontSize="28" FontWeight="Bold" Foreground="Red" HorizontalAlignment="Center"/>
                    <TextBlock Text="已停止" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardStyle}">
                <StackPanel>
                    <TextBlock Text="{Binding AvgResponseTime}" FontSize="28" FontWeight="Bold" Foreground="Blue" HorizontalAlignment="Center"/>
                    <TextBlock Text="平均响应(ms)" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- 工具栏 -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" Text="{Binding FilterMetricType, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalContentAlignment="Center" ToolTip="按指标类型过滤" HorizontalAlignment="Left"/>
            <Button Grid.Column="1" Content="刷新" Command="{Binding RefreshCommand}" Margin="0,0,4,0" Width="70"/>
            <Button Grid.Column="2" Content="清除" Command="{Binding ClearFilterCommand}" Width="70"/>
        </Grid>

        <!-- 数据库状态 -->
        <Border Grid.Row="2" Background="{DynamicResource MesCardBackgroundBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="数据库连接状态" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,6"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="状态: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding DatabaseStatus.Status}" FontSize="12" Margin="0,0,16,0"/>
                    <TextBlock Text="数据库: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding DatabaseStatus.DatabaseName}" FontSize="12" Margin="0,0,16,0"/>
                    <TextBlock Text="连接池: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding DatabaseStatus.ActiveConnections}" FontSize="12"/>
                    <TextBlock Text="/" FontSize="12"/>
                    <TextBlock Text="{Binding DatabaseStatus.MaxConnections}" FontSize="12" Margin="0,0,16,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="平均查询时间: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding DatabaseStatus.AvgQueryTime}" FontSize="12" Margin="0,0,16,0"/>
                    <TextBlock Text="慢查询: " Foreground="{DynamicResource MesTextSecondaryBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding DatabaseStatus.SlowQueries}" FontSize="12"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- 服务状态表格 -->
        <DataGrid Grid.Row="3" ItemsSource="{Binding Services}"
                  AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" GridLinesVisibility="Horizontal" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="服务名称" Binding="{Binding ServiceName}" Width="150"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="响应时间(ms)" Binding="{Binding ResponseTime}" Width="100"/>
                <DataGridTextColumn Header="端点" Binding="{Binding Endpoint}" Width="200"/>
                <DataGridTextColumn Header="版本" Binding="{Binding Version}" Width="100"/>
                <DataGridTextColumn Header="请求数" Binding="{Binding RequestCount}" Width="80"/>
                <DataGridTextColumn Header="错误数" Binding="{Binding ErrorCount}" Width="80"/>
                <DataGridTextColumn Header="错误率(%)" Binding="{Binding ErrorRate, StringFormat=F2}" Width="80"/>
                <DataGridTextColumn Header="最后心跳" Binding="{Binding LastHeartbeat, StringFormat=yyyy-MM-dd HH:mm:ss}" Width="140"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 错误提示 -->
        <TextBlock Grid.Row="3" Text="{Binding ErrorMessage}" Foreground="Red" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" TextWrapping="Wrap"/>
    </Grid>
</UserControl>
