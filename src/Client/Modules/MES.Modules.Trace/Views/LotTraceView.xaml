<UserControl x:Class="MES.Modules.Trace.Views.LotTraceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource MesBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="批次追溯" FontSize="20" FontWeight="Bold" Foreground="{StaticResource MesTextPrimaryBrush}" Margin="0,0,0,12"/>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{StaticResource MesSurfaceBrush}" Foreground="{StaticResource MesTextPrimaryBrush}"
                  BorderBrush="{StaticResource MesBorderBrush}" RowBackground="{StaticResource MesCardBrush}"
                  AlternatingRowBackground="{StaticResource MesSurfaceBrush}" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="工序" Binding="{Binding ProcessName}" Width="120"/>
                <DataGridTextColumn Header="设备" Binding="{Binding EquipmentId}" Width="100"/>
                <DataGridTextColumn Header="配方" Binding="{Binding RecipeId}" Width="100"/>
                <DataGridTextColumn Header="进站时间" Binding="{Binding StartTime, StringFormat='{}{0:HH:mm:ss}'}" Width="100"/>
                <DataGridTextColumn Header="出站时间" Binding="{Binding EndTime, StringFormat='{}{0:HH:mm:ss}'}" Width="100"/>
                <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
