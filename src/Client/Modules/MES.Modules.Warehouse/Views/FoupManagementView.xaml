<UserControl x:Class="MES.Modules.Warehouse.Views.FoupManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource MesBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="载具管理" FontSize="20" FontWeight="Bold"
                   Foreground="{StaticResource MesTextPrimaryBrush}" Margin="0,0,0,12"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Background="{StaticResource MesSurfaceBrush}" Foreground="{StaticResource MesTextPrimaryBrush}"
                  BorderBrush="{StaticResource MesBorderBrush}" RowBackground="{StaticResource MesCardBrush}"
                  AlternatingRowBackground="{StaticResource MesSurfaceBrush}" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="载具号" Binding="{Binding CarrierId}" Width="100"/>
                <DataGridTextColumn Header="位置" Binding="{Binding Location}" Width="120"/>
                <DataGridTextColumn Header="批次" Binding="{Binding LotId}" Width="120"/>
                <DataGridTextColumn Header="洁净度" Binding="{Binding Cleanliness}" Width="100"/>
                <DataGridTextColumn Header="Strip数" Binding="{Binding StripCount}" Width="80"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
