<UserControl x:Class="MES.Modules.Warehouse.Views.InventoryManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{DynamicResource MesBackgroundPrimaryBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 页面标题 -->
        <TextBlock Grid.Row="0" Text="库存管理" Style="{DynamicResource MesPageTitle}" Margin="0,0,0,16"/>

        <!-- 搜索和统计区域 -->
        <Border Grid.Row="1" Style="{DynamicResource MesCard}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="搜索物料:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,16,0"/>
                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="库存总值: " VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="{Binding TotalValue, StringFormat='{}{0:C2}'}" FontWeight="Bold" Foreground="LightGreen" VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="刷新数据" Command="{Binding RefreshCommand}" Padding="12,4"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 库存状态卡片 -->
        <Border Grid.Row="2" Style="{DynamicResource MesCard}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="物料种类" HorizontalAlignment="Center" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding FilteredView.Count}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Foreground="LightBlue"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="低于最低库存" HorizontalAlignment="Center" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Foreground="Red">
                        <TextBlock.Text>
                            <Binding Path="Items">
                                <Binding.StringFormat>{0}</Binding.StringFormat>
                            </Binding>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="库存总值" HorizontalAlignment="Center" FontSize="12" Foreground="{DynamicResource MesTextSecondaryBrush}"/>
                    <TextBlock Text="{Binding TotalValue, StringFormat='{}{0:C0}'}" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" Foreground="LightGreen"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 库存数据表格 -->
        <Border Grid.Row="3" Style="{DynamicResource MesCard}" Padding="16" Margin="0,0,0,0">
            <DataGrid ItemsSource="{Binding FilteredView}" SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="物料编号" Binding="{Binding MaterialNo}" Width="120"/>
                    <DataGridTextColumn Header="物料名称" Binding="{Binding MaterialName}" Width="150"/>
                    <DataGridTextColumn Header="当前库存" Binding="{Binding CurrentStock}" Width="100"/>
                    <DataGridTextColumn Header="最低库存" Binding="{Binding MinStock}" Width="100"/>
                    <DataGridTextColumn Header="最高库存" Binding="{Binding MaxStock}" Width="100"/>
                    <DataGridTextColumn Header="单位" Binding="{Binding Unit}" Width="60"/>
                    <DataGridTextColumn Header="存放位置" Binding="{Binding Location}" Width="120"/>
                    <DataGridTextColumn Header="库存总值" Binding="{Binding TotalValue, StringFormat='{}{0:C2}'}" Width="120"/>
                    <DataGridTemplateColumn Header="库存状态" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="正常"/>
                                            <Setter Property="Foreground" Value="Green"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CurrentStock}" Value="{Binding MinStock, RelativeSource={RelativeSource Self}}">
                                                    <Setter Property="Text" Value="库存不足"/>
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
