<UserControl x:Class="MES.Modules.Yield.Views.YieldDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             Background="{StaticResource MesBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="良率看板" FontSize="20" FontWeight="Bold" Foreground="{StaticResource MesTextPrimaryBrush}" Margin="0,0,0,12"/>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding Items}">
            <ItemsControl.ItemsPanel><ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate></ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{StaticResource MesCardBrush}" CornerRadius="8" Padding="24,20" Margin="8" MinWidth="200" BorderBrush="{StaticResource MesBorderBrush}" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="{Binding KpiName}" Foreground="{StaticResource MesTextSecondaryBrush}" FontSize="14"/>
                            <TextBlock Text="{Binding Value}" Foreground="{StaticResource MesAccentBrush}" FontSize="32" FontWeight="Bold" Margin="0,8,0,4"/>
	                            <TextBlock Text="{Binding Unit}" Foreground="{StaticResource MesTextMutedBrush}" FontSize="12" Margin="0,0,0,2"/>
	                            <TextBlock Text="{Binding Trend}" Foreground="{StaticResource MesTextSecondaryBrush}" FontSize="12"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
